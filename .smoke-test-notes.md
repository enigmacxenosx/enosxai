# Smoke test observations

- The repository has no committed or configured production `VITE_NEON_API_URL` value and no public API-server URL in tracked configuration.
- The home route `/` reaches the splash screen and then renders the chat landing screen.
- The splash and welcome screen use the shared EX logo; the previous large red bubble and removed shortcut/sign-in controls are absent.
- The home interface exposes sidebar navigation, sign-in, new chat, chat history, screen guide, GitHub, About, file upload, voice input, image mode, and send controls.
- The composer placeholder is an intentional input hint (`Ask ENOSX AI anything.`), not an unfinished page placeholder.

The `/about` route navigates successfully after the splash transition. It renders the vision, story, capabilities, founder, stack, contact, and footer sections. No unfinished placeholder copy was visible; the example-like capability labels are intentional product descriptions.

The explicit `/404` route renders a neutral static not-found card with a working `Go Home` button. The cleaned screen has no oversized red bubble or pulsing error decoration.

The 404 `Go Home` recovery returned to `/`. The expanded sidebar rendered New Chat, Chat History, Screen Guide, GitHub, About ENOSX, and Sign In controls. New Chat reset the workspace and kept the user on the home route without an error.

Chat History opened a search overlay with an intentional empty-state prompt and a working close control. No broken route or unfinished placeholder was observed.

Screen Guide activated its visible status toast and changed the welcome prompt without breaking the route. The profile panel opened with the cleaned email sign-in form and no Google button; its current default auth path is the local fallback because no remote API URL is configured.

The empty Create Account button was disabled as expected. A synthetic sign-up then succeeded and opened the authenticated profile panel with display name, avatar, AI personality, settings, upgrade, save, and sign-out controls visible.

Sign-out returned the panel to the create-account form, and the same synthetic account then signed in successfully and restored the profile view. No real personal credentials were used.

GitHub opened a clear empty-state panel with an Add Account control and did not require entering a real token. Voice input failed gracefully with the explicit message that no microphone was found. Image-generation mode toggled visibly in the composer without a crash.

The upload control is wired to a single accept-filtered file input covering common image, text, code, document, spreadsheet, and presentation formats; no personal file was selected.

A harmless chat prompt reached the UI end-to-end but the local smoke-test environment returned `API Error: 500`. The frontend rendered the intended friendly copy, an amber `Connection issue` card, and a `Retry Transmission` action rather than the removed red-bubble treatment. This confirms the remaining issue is backend/API availability or configuration, not an unhandled frontend crash.

The About ENOSX sidebar link navigated successfully to `/about`, and the Back to Chat link returned to `/`. An unknown route (`/definitely-not-a-real-route`) rendered the neutral 404 page with a working Go Home button and no red bubble.

Production verification: `https://enosxai.vercel.app/` reached the deployed chat home after the splash transition with the glass EX mark and no red bubble. `https://enosxai.vercel.app/about` rendered the full About page and its navigation links successfully.

Repository validation: `pnpm build` passed typechecking and production builds for enosx-app, exlover, and api-server. The smoke-test notes were committed and pushed after rebasing onto the latest remote changes; final main is clean and synchronized. The synchronized main state passed the final `pnpm build` run.
