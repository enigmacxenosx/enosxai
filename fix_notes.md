# CI lockfile mismatch fix — RESOLVED
CI "CI — pnpm Workspace Checks" now PASSES (run 31956657086, commit 82a918f). Actions taken: removed stale `overrides:` block + pnpm v10-only fields (minimumReleaseAge*, onlyBuiltDependencies) from pnpm-workspace.yaml (CI uses pnpm v9 which doesn't parse them); regenerated pnpm-lock.yaml with pnpm 9.15.9; fixed workflow filter name to `@workspace/enosx-app`. Local typecheck + vite build pass.

# STILL PENDING: Vercel has NOT redeployed since 15:31:56 despite 4 successful pushes (toggle deploy happened earlier).
Live bundle index-AUQDs3Ne.js (fetched as /tmp/deployed.js) contains NO runPythonAsync (0 matches), only 2 Terminal matches. Vercel buildCommand is `pnpm run build` in root, outputDirectory enosx-app/dist/public, framework vite. Possible cause: Vercel project setting to skip deploy when checks fail? Or deploy is actually queued/slow. Next: wait longer (~5 min), re-check last-modified; if still stale, check via `gh api repos/.../statuses` or trigger deployment by pushing an empty commit; also could check Vercel dashboard deployment list. Note: my earlier toggle verification found the toggle live (15:21), so Vercel DID deploy at that time. The script-console code (3 pushes since 15:26) never deployed.

The `overrides` block exists only in `pnpm-workspace.yaml` (a stale left-over from a template) and no longer in any `package.json`. pnpm's frozen install compares the workspace-level overrides against the lockfile's recorded overrides; the lockfile (lockfileVersion 9.0) was generated when `package.json` held the overrides, so the recorded set differs from the workspace.yaml set and the frozen install aborts with ERR_PNPM_LOCKFILE_CONFIG_MISMATCH.

Fix: remove the entire `overrides:` section from `pnpm-workspace.yaml` and regenerate the lockfile with the same pnpm major (v9, matching CI) so the recorded settings match. Using pnpm 11 risks changing lockfile version; pin to 9 for the regen to keep the diff minimal.

## Vercel deploy status (16:00)
Commit statuses on 82a918f: "Vercel – exlover failure: Deployment has failed (dpl_BeHdi2MDQCZbpoJtucZsApWozden)", "Vercel – jjjjj-4tkd failure: Deployment rate limited — retry in 24 hours.", "Vercel – exlover pending: Vercel is deploying your app". So Vercel deployments FAILED/rate-limited for this repo's deployment group ("enosxai" site may be separate from exlover). The enosxai.vercel.app site itself hasn't redeployed since 15:31. Hypothesis: the enosxai deployment group is stuck/failed. Options: (1) retry via vercel CLI if token exists (none), (2) push empty commit later to retry, (3) check if enosxai vercel project has auto-cancel/deploy settings. Note rate-limit may lift. Meanwhile proceed with the chat-page toggle feature; deployment retry will pick it up.
