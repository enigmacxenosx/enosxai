# State 12 — READ THE ACTUAL CODE, analysis on paper contradicts observation

Only "line 999" rendered for a 1000-line run → pending flush is not merging all lines. Must re-read useScriptRuntime.ts lines around appendRunOutput/flush to see the real code (possibly my in-context mental model drifted after compaction).
